apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
spec:
  encryptedData:
    api-token: xxxxxxxxxx
  template:
    metadata:
      name: cloudflare-api-token
      namespace: cert-manager
