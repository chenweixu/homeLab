apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-grafana
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-home-sc
