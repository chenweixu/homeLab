apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cwx

resources:
  - Deployment.yaml
  - IngressRoute.yaml
  - Service.yaml

configMapGenerator:
  - name: blog-config-nginx
    files:
      - config/nginx.conf
